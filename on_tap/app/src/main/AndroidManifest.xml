<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.on_tap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.03_19529651_PhamDangDan">
        <activity
            android:name="com.example.on_tap.HomeActivity"
            android:exported="false" />
        <activity
            android:name="com.example.on_tap.RegisterActivity"
            android:exported="false" />
        <activity
            android:name="com.example.on_tap.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>